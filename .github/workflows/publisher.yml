name: Javascript Publisher test 

on:
  push:


jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Install dependencies
      run: |
        npm install jest
        npm install xmlhttprequest
    - name: Run Jest on target
      run: npm run test testing/publisher.test.js
